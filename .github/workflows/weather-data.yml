name: Weather Data Collection

on: 
  # Trigger the workflow manually
  workflow_dispatch:

jobs:
  collect_weather_data:
    runs-on: ubuntu-latest
    
    steps:
      # Output weather data using a shell script
      - name: Output weather data
        run: ./weather.sh




